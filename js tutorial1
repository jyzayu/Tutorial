const player = {
    name:"CJY",
    fat:"medium",
    points:60,
};
object 와 list의 차이점 과 object 선언, 값 수정, 
// player=false 는 에러지만  player.name= "C" 는 가능하다.
//object는 리스트와 달리 name fat points등 각 요소들이 의미를 갖는다  여러요소를 묶어 저장하는것은 비슷하다. 
 list는 여러요소를 넣었는데 그것이 무엇을 의미하는지 파악하는데 불편함이 있다. object는 무엇을 의미하는지
// 식별할수있다. list는 파악하려면 주석으로 설명해야하는데 object는 그럴 필요가 없다 
const player ={
    name:"CJT"
 } 
와 같이 const 로 정의된 object 는 새로운 property 를 추가할수도 있고 , property의 값을 변경할수도 있다. 각요소를 property라고 부른다.
// but player=false 와 같이 object의 모든 property의 값을 한번에 바꾸려고하는 것은 불가능하다. 




Array
// string false null undefined 
// 데이터 정리하는 법 검색이나 삽입을 빨리 할수있는
// 데이터 저장방법 기본적인 정리방법

Searching For Elements
querySelector 는 css방식으로 html 의 element를 검색할 수 있다.
클래스안에있는 태그element를 가져올수있다.
querySelector(".hello h1");  hello class안에 있는 h1을 가져올수있다.  클래스네임 앞에.을붙여 접근하는 방식은 css에서 사용했었다. 
getElementByClassName("hello") 이방식을 사용할때는 클래스네임앞에 .을 붙이지 않았다. 
queryselector 자주사용. 클래스네임앞에 . 을붙여줘야 클래스로 알수있다. 단 하나의 값을 리턴한다.
만약 같은 클래스의 같은 태그가 여러개있다면 가장 맨위에있는 값을 리턴한다. 
같은 것 여러개있을때 querySelectorAll 을 사용하면 모든 element가 담겨있는 array를 리턴한다.
id를 이용해 element를 검색할때도 quertSelector("#hi"); 등 #을붙여 id를 이용해 검색한다.  대부분 queryselector를 사용한다.
queryselector를 이용해 첫번째 element의 하위에 속한 element 를 두번째에 넣어 상세하게 검색할수있다. 
console.dir("headline"); 을통해 headline을 object형식으로 보여준다. 
여기서 onpaste onclick 등 on ~ 으로 표시된것들을 이벤트라고한다.
ㅁ:ㅇ 꼴로 나타난것들은 모두 object들이다
이 object들은 변경할수있다.
