h,p ,hr ,br ,&nbsp; 
img src="" alt="" width,height, title
container div , span
전역속성 id , class   -js css식별자
a href="" target="_self" 
li ol ul
input = "" type="" name=""  - js,css
select
html tag가 태그를 정의하는 계단식구조여서
이를 고려해 계단식으로 스타일을 정의하는 문서 css

<!DOCTYPE html>  <!-- html start it has various tagc-->
    <head>  
        <meta charset="utf-8"> 한글영어 둘다 사용
         메타데이터 검색엔진에 내용이나 해시태그처럼 적용되고 사용자 이용할떄 어떤 브라우저 버전으로 적용시키는지 ;
         <meta http-equv="     IE="edge"
         <meta keywords="animal , cute, challenge"
        <meta name="viewport" content="width=device-width, initial-scale=1.0"> 모바일로 봐도 컴퓨터웹과 비슷하게 축소되지 않고 핸드폰넓이에 맞춰서 나타냄
        <title>title of document </title>     
    </head>

    <body>  
        <!-- text img  interface (button input ) 등 웹에 보여지게 나타난다.
        cheer
        <div id="study">
        <h1 class="title">     studying economy is important  </h1>   헤더제목 큰텍스트   한칸 띄운다음 써진다 다음부터
        <img src="images/병적증명서.png" alt="병적증명서" width="300" height="300" title="병적증명서">  이미지 나타내는법 title은 툴팁 마우스 갖다대면 뜬다ㅡ. alt는 이미지 에러시
        <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcS1fpNPbM6MV04TcFouRM9z7Dzd2c_ReAjqpA&usqp=CAU" 웹에서 이미지주소복사해서 나타낼수도있다.
        alt="도라에몽이미지"  width="300" height="300" title="도라에몽귀여워">
        </div>

        <div id="workout">    div에는 class 쓰던데
        <h1 class="title"> just do it</h1>
        <p> do workout is important<br> 
        </p>

        <hr>
        <div>
            <h1 class="title"> 
            </h1>
            <a href="https://www.naver.com/" target="_blank"> 네이버로갑시다! </a><br>   링크만들기 인라인요소라 띄어쓰기 br해줘야함 주소 복사해서 넣어주면 됨 
            <a href="file:///C:/Users/mooso/Desktop/sample/test.html" target="_blank">my html</a><br>  타겟은 새로운창 또는 현재창에서열꺼냐 
            <a href="mailto:moosou30@naver.com">email</a><br>   전화나 이메일등도 할수있다 .
            <a href="tel:010-8718-0243">010-8718-0243</a> 
            <input name="text" type="text" maxlength="5"        placeholder 아무것도 안적었을떄 뜨는거 
             placeholder="input your name" name="name"> <br><br>

            <input name="push" type="button" value="PUSH" name="PUSH"><br>SELECT COLOR<br>  인풋은 버튼 컬러 레인지 날짜 , select textarea등 다양하게 있다
             인풋에 네임 속성을 붙여 js나 css할떄 입력값을 처리ㅏ하는데 쓰여 필수다 . 네임들어가는데ㅐ 또 어디지  메타?
             
            <input name="color" type="color" min="0" max="100" step="10"><br><br>

            <input name="score" type="range" ><br><br>

            <input name="birth of date" type="date"> <br><br>
            </div >

        </div>
    </body>
</html> 

   <!-- h1 ~ h6 headline of bodyw
   p i s paragraph it has new line when ended.
   hr mean making headline to distinguish
   js ignore enter and space so we use br and &nbsp ; 
     -->
<!-- tag block level : 자기가 속한영역의 너비를 모두 차지해 블록형성
and inline : 자기에게 필요한 만큼만 차지
개발자도구는 현재 보고있는 html문서를 볼수 있다, 개발자도구열면
각 코드에 마우스를 대면 태그가 얼마만큼 영역을 차지하는지 보여주낟 
h,p is block level  글이 끝난 오른쪾 모든영역을 다 차지하고있음 --> -->
<!-- strong,em,mark is inline it is included in block level h,p
inline doesnt affect on block of h,p so it is used 
in blocklevel h,p  if define block p in  p 
block 안에 층이 생긴다. -->
<!-- img alt 이미지 유실되는 상황에 대응 하기위한 대체텍스트
block container div 주제나눈 덩어리를 묶어놓아 
it make better to do css stylesheet or js interaction
hr is used to distinguish 주제
inline container span 특정키워드 묶어 블록컨테이너같이
js css 적용에 편리하다.  -->
<!-- define name is used to js,server programming
사용자가 입력한 데이터를 처리할떄ㅑ 쓰인다 -->

css작성 태그안에 속성값으로 하면 유지보수힘듬 시에스파일 따로만드는게 좋다
스타일 태그를 추가하거나 
헤드에 링크 태그를 추가하여 스타일싯문서 따로만든다.
연결한다해ㅓㅅ 링크 어떤걸연결할건가 href 
 rel연결하고자하는 문서가 현재문서랑 어떤관계인지
css 선택자 중괄호 속성명 속성값으로 구성
주석이 있다
인라인 스타일태그 외부문서 연결 

선택자: 어떤요소에 스타일을 적용 할 것 인지에대한 정보
기본 그룹, 특성, 결합선택자
의사클래스 의사요소

클래스는 html 모든요소에 적용시킬수있는 전역속성
.text {
	color:blue;
} 문서내 class가 text 인 모든 요소 
클래스는 동일한 클래스속성값을 가진 요소가 여러개 있을 수있지만
id는 고유한 식별자 역할을 하는 전역속성이라 해당id를 가진 요소는 한가지
#topic { color:blue;}

그룹선택자
쉼표를 이용해 선택자를 그룹화한다.
h1,p,div{ color:blue; }	 
선택자 겹치는 경우 기본적으로 나중에 작성된스타일이 적용
선택자 다르지만 요소가 겹치는 경우 ,선택자 우선순위에의해
적용될 스타일이 결정된다. 
1아이디2 클래스 3태그선택자 순서로 우선순위를 갖는다.

font-family serif, monospace
font-size px,rem,em
text-align L R Center Justify
 color(rgb)
inline-block block의 속성을 가질수있다 
인라인은 자기영역만차지하기떄문에 width height를 조정안하지만
블럭은 가지는데 이를 인라인에 적용시키고싶을떄 사용
어느 영역을 차지하는지 알아내는 용도로
border사용해 레이아웃 배치를 결정하는데 사용하기도한다.

div 
span 인라인 블록 차이 위스 하이트가 인라인에는 지정안됨
